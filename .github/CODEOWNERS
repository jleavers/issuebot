# A change to these paths is a change to what every unattended issuebot session inherits (#107):
# the instruction files a session is handed, the workflow that governs it, and the skills and
# workflows a developer's own claude loads from this checkout. Review one as a change to the
# agent's privileges, not as documentation.
/CLAUDE.md     @jleavers
/AGENTS.md     @jleavers
/.claude/      @jleavers
/configs/      @jleavers
# And the file that says so, plus the workflows that run on every pull request: a change to
# either is the first step of editing the rest unrouted.
/.github/      @jleavers
