# The vendored front-end files are served, and checked (tests/test_web_vendor.py), byte for byte:
# never translate their line endings on checkout, whatever the host's core.autocrlf says (#108).
src/issuebot/web/static/vendor/*.js -text
